export enum ScannedStatus {
	SCANNED = 'scanned',
	UNSCANNED = 'unscanned'
}

export enum RFIDDataType {
	INBOUND = 'inbound',
	OUTBOUND = 'outbound'
}

export enum ScanCapability {
	SCANNABLE = 'scannable',
	UNSCANNABLE = 'unscannable'
}
