import { createFileRoute } from '@tanstack/react-router';

export const Route = createFileRoute('/(features)/_layout/warehouses/')({
	component: () => <div>Hello /(features)/_layout/warehouse/!</div>
});
