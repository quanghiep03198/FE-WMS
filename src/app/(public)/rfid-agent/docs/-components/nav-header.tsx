import { Div } from '@/components/ui'

type Props = {}

const NavHeader = (props: Props) => {
	return (
		<Div as='header' className='bg sticky top-0'>
			NavHeader
		</Div>
	)
}

export default NavHeader
